# Platform release packs

This directory contains platform-specific files and instructions only. Platform-agnostic scripts, B-roll prompts, thumbnail specifications, and brand rules belong in `content_core/`.

## Identity

- YouTube channel: **The Tyrrell Codex**
- Series: **Ferrum Doctrine**
- Channel ID: `UCh82kLGeSgdCd1VNEQsZ2pg`
- Rename lock: do not rename again without a new direct user instruction.

## Operator order

1. Open the platform subfolder.
2. Read `publish_manifest.json` for the exact file, copy, tags, and scheduled time.
3. Read `WHAT_WHERE_WHEN.md` for the human checklist.
4. Confirm the correct account and channel immediately before any upload.
5. Record a post as `published` only after the platform returns a public or scheduled URL.

The master clock is `SCHEDULE.json`. Its times are provisional until account-level audience analytics exist.

## Current truth

- YouTube, Instagram, TikTok, and X release units are locally ready and not published.
- Reddit text is drafted, but community rules must be reviewed at posting time.
- Buffer cycles 002–004 remain on QA hold even where local render files exist.
